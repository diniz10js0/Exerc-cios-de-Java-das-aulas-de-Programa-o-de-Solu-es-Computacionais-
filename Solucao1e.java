import javax.swing.JOptionPane;
public class Solucao1e {
    public static void main(String[] args) {
        // Definindo os dez caracteres diretamente no código
        char[] caracteres = {'C', 'a', 'l', 'v', 'e', 't', 't', 'i', '!', '!'};

        // Construindo a palavra a partir dos caracteres
        String palavra = new String(caracteres);

        // Exibindo a palavra formada pelos caracteres
        System.out.println(palavra);
    }
}
